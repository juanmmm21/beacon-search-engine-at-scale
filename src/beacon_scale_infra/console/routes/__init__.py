"""Rutas de la API de la consola (fase 6). Mismo contrato versionado
`/api/v1/...` que `beacon-search-console`, con sus mismos modelos de
respuesta importados del paquete real de la consola -- el frontend React de
ese repo corre sin cambios contra esta API (ver `ARCHITECTURE.md`, fase 6)."""
